[project]
name = "traitement"
version = "0.1.0"
description = "traitement service dependencies"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.136.3",
    "sqlalchemy>=2.0.51",
]
[dependency-groups]
dev = [
    "ruff>=0.15.17",
]
